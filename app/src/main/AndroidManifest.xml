<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />
    <!-- All files access permission for Android 11+ -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <application
        android:name=".OtterApplication"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Otter"
        android:requestLegacyExternalStorage="true">

        <!-- Otter - Auto-extraction activity -->
        <activity
            android:name=".ExtractionActivity"
            android:exported="true"
            android:theme="@style/Theme.Otter">
            <!-- Intent filter for "Open with" archives - Auto-extraction (content URIs) -->
            <intent-filter
                android:label="Otter (Extract here)"
                tools:ignore="AppLinkUrlError">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="content" />
                <data android:mimeType="application/zip" />
                <data android:mimeType="application/x-zip-compressed" />
                <data android:mimeType="application/rar" />
                <data android:mimeType="application/x-rar-compressed" />
                <data android:mimeType="application/vnd.rar" />
                <data android:mimeType="application/x-rpa" />
            </intent-filter>

            <!-- Intent filter for "Open with" RPA files (file URIs) -->
            <intent-filter
                android:label="Otter (Extract here)"
                tools:ignore="AppLinkUrlError">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="file" />
                <data android:mimeType="*/*" />
                <data android:pathPattern=".*\\.rpa" />
                <data android:pathPattern=".*\\..*\\.rpa" />
                <data android:pathPattern=".*\\..*\\..*\\.rpa" />
            </intent-filter>

            <!-- Intent filter for content URIs with any MIME (fallback for unknown types) -->
            <intent-filter
                android:label="Otter (Extract here)"
                tools:ignore="AppLinkUrlError">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="content" />
                <data android:mimeType="*/*" />
                <data android:pathPattern=".*\\.rpa" />
                <data android:pathPattern=".*\\..*\\.rpa" />
                <data android:pathPattern=".*\\..*\\..*\\.rpa" />
            </intent-filter>
        </activity>

        <!-- Otter - File browser activity (main) -->
        <activity
            android:name=".BrowserActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Otter">
            <!-- Launcher intent to open the app -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Intent filter for "Open with" archives - Browse mode (content URIs) -->
            <intent-filter
                android:label="Otter (Browse)"
                tools:ignore="AppLinkUrlError">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="content" />
                <data android:mimeType="application/zip" />
                <data android:mimeType="application/x-zip-compressed" />
                <data android:mimeType="application/rar" />
                <data android:mimeType="application/x-rar-compressed" />
                <data android:mimeType="application/vnd.rar" />
                <data android:mimeType="application/x-rpa" />
            </intent-filter>

            <!-- Intent filter for "Open with" RPA files - Browse mode (file URIs) -->
            <intent-filter
                android:label="Otter (Browse)"
                tools:ignore="AppLinkUrlError">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="file" />
                <data android:mimeType="*/*" />
                <data android:pathPattern=".*\\.rpa" />
                <data android:pathPattern=".*\\..*\\.rpa" />
                <data android:pathPattern=".*\\..*\\..*\\.rpa" />
            </intent-filter>

            <!-- Intent filter for content URIs with any MIME - Browse mode (fallback for unknown types) -->
            <intent-filter
                android:label="Otter (Browse)"
                tools:ignore="AppLinkUrlError">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="content" />
                <data android:mimeType="*/*" />
                <data android:pathPattern=".*\\.rpa" />
                <data android:pathPattern=".*\\..*\\.rpa" />
                <data android:pathPattern=".*\\..*\\..*\\.rpa" />
            </intent-filter>
        </activity>

        <!-- Extraction Service -->
        <service
            android:name=".service.ExtractionService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- FileProvider for opening extracted folders -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
